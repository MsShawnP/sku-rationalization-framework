# Lailara LLC — Engagement config (DEMO). Canonical demo identity; demo: true so
# the deploy guard ignores it. A real client run uses engagement.yml with the
# client's own SKU portfolio.
client:
  name: "Cinderhaven Provisions (demo)"
engagement:
  id: "CINDERHAVEN-DEMO"
as_of_date: "2026-01-31"
prepared_by: "Lailara LLC"
demo: true
basis:
  window_label: "2023–2026"
  # Optional per-dimension weights override (default is equal 0.2 each).
columns:
  sku: "sku"
  product_line: "product_line"
  uspw: "uspw"
  loaded_margin_pct: "loaded_margin_pct"
  annual_shelf_space_cost: "annual_shelf_space_cost"
  complexity_ratio: "complexity_ratio"
  cannibalization_risk: "cannibalization_risk"
