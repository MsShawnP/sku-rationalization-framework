server {
    listen 8080;

    location / {
        root /srv/app;
        try_files $uri $uri/ /index.html;
    }

    location /data/ {
        alias /srv/data/;
    }

    location /health {
        access_log off;
        return 200 'ok';
        add_header Content-Type text/plain;
    }
}
